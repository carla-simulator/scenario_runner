py-trees==0.8.3
numpy
networkx
Shapely
psutil
xmlschema==1.0.18
ephem
tabulate
opencv-python
matplotlib
six
graphviz
# Optional: only required for OpenSCENARIO 2.0 (--openscenario2) support.
# The 4.10 runtime conflicts with packages that pin antlr4 4.9.* (e.g.
# omegaconf); without it every other scenario_runner mode still works.
# antlr4-python3-runtime==4.10
