struct traffic_light:
    event car_arrived                             # An event with no parameter
    event accident_occurred(other_car: vehicle)   # An event with a single parameter
    event e5 is @e0 if every (3s)              # An event with a every condition
    event e5 is @e0 if every (3s, offset: 1s)              # An event with a every condition
